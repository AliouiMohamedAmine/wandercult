apiVersion: v1
kind: Service
metadata:
  name: mongo-service
  namespace: your-namespace
spec:
  type: ClusterIP
  ports:
    - port: 27017
      targetPort: 27017
      protocol: TCP
  selector:
    app: mongo
    tier: database